### Cloudflare Workers & Pages

- **Workers Runtime:** Deep understanding of V8 isolates, cold starts, CPU limits, memory constraints, and runtime APIs
- **Wrangler CLI:** Mastery of configuration (`wrangler.jsonc`), local development, deployment, and type generation via `wrangler types`
- **Environment Bindings:** Implementation of KV storage, Durable Objects, R2 object storage, D1 databases, and Queues
- **Vite Integration:** Configuration of `@cloudflare/vite-plugin` for SSR, environment handling, and build optimization
- **Request Handling:** Expert use of `fetch` handler pattern, `Request`/`Response` APIs, and streaming responses
- **Edge Configuration:** Compatibility flags (`nodejs_compat`), compatibility dates, and feature targeting
- **Environment Variables:** Secure handling of secrets via `wrangler secret`, typed `Env` interfaces, and `vars` configuration
- **Observability:** Implementation of Workers Analytics, logging patterns, and integration with external monitoring services
- **Smart Placement:** Understanding of automatic geographic placement for latency optimization
- **Service Bindings:** Inter-worker communication, service composition, and microservices patterns on the edge
